# 📒 Notes App

A Restful Notes Application built using **Python**, **FastAPI**, **Bootstrap** and **MongoDB** — designed for creating, reading, updating, and deleting notes with a clean API and optional frontend support.

This project provides a scalable backend service to manage notes (text content) via a modern FastAPI server and stores the data in a MongoDB database. FastAPI makes the API fast and easy to document with built-in OpenAPI support.

---

## 🚀 **Tech Stack**

| Layer              | Technology        |
|------------------|------------------|
| Backend Framework | Python + FastAPI |
| Database          | MongoDB          |
| API Docs          | Swagger   |
| ORM / ODM         | PyMongo |
| Deployment        | Docker (optional)|

---

## 🧠 **Project Description**

The Notes App is a simple but powerful API for managing notes. Users can add new notes, read existing ones, update them, or delete unwanted entries. The Restful design makes it ideal for use with frontend clients (web, mobile, or others), and the API documentation is auto-generated by FastAPI.

**Features include:**

- 📝 Create rich text notes
- 🔎 Retrieve all or individual notes
- ✏️ Update notes
- ❌ Delete notes
- 📄 Interactive API docs via Swagger UI

This backend can also be extended with user authentication, frontend integration, search, pagination, tagging, and more.

---

## 🛠 **Getting Started**

### 📋 Prerequisites

Make sure you have the following installed:

- Python 3.10+
- pip 23.1.2
- MongoDB (local or Atlas cloud)
- pip or equivalent package manager

---

## Create a virtual environment

python -m venv venv  

venv\Scripts\activate         # On Windows

python.exe -m pip install --upgrade pip    #upgrade pip

## 🎯 **Installation**

pip install "fastapi[standard]"

pip install "uvicorn[standard]"

## Run the server

uvicorn main:app --reload

uvicorn index:app --reload

## Other installation

pip install jija2
pip install pymongo
pip install python.multipart

1. **Clone the repository**

   ```bash
   git clone https://github.com/Atanu562/Notes_App.git
   cd Notes_App

## Project Structure

.
├── main.py            # FastAPI application bootstrap

├── routes/            # API route handlers

├── models/            # Database models / schemas

├── schemas/           # Pydantic schemas

├── config/            # DB & env setup

├── requirements.txt   # Dependencies

├── static/            # Static files (optional)

├── templates/         # HTML templates (optional)

└── README.md

## API Endpoints

Once running, you can visit:

💡 Swagger UI: http://localhost:8000/docs
**Example CRUD routes:**

| Method | Endpoint      | Description       |
| ------ | ------------- | ----------------- |
| GET    | `/notes/`     | List all notes    |
| POST   | `/notes/`     | Create a new note |
| GET    | `/notes/{id}` | Get note by ID    |
| PUT    | `/notes/{id}` | Update note       |
| DELETE | `/notes/{id}` | Delete note       |

